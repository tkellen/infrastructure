#!/usr/bin/env bash
# Load secrets (pki, encryption keys, etc) from remote storage.
set -euo pipefail

KWM_CLUSTER_NAME=${KWM_CLUSTER_NAME:-'must be defined.'}

aws s3 sync s3://infrastructure-state-store/$KWM_CLUSTER_NAME state
