#!/usr/bin/env bash
# Save secrets (pki, encryption keys, etc) to remote storage.
set -euo pipefail

KWM_CLUSTER_NAME=${KWM_CLUSTER_NAME:-'must be defined.'}

aws s3 sync secrets s3://infrastructure-state-store/$KWM_CLUSTER_NAME
